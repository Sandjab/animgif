# AnimGIF v2 (lot 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter reverse, flip 3D, décimation de frames et 5 filtres natifs, et rembourser les deux dettes UX (« n fois » en preview, rendu d'export bloquant/non annulable).

**Architecture:** Aucun changement structurel : nouveaux effets = nouvelles matrices, nouvelles fonctions pures dans `timeline.ts`, extension de `buildFilterString`, et rework du pipeline d'export (rendu par tranches + annulation). Spec : `docs/superpowers/specs/2026-07-08-animgif-v2-design.md`.

**Tech Stack:** identique v1 (Vite + TS vanilla, Vitest, gifski-wasm en worker).

**État de départ (vérifié le 2026-07-08, HEAD = cfe3bc0) :** 31 tests verts. `export.ts` contient le hotfix ping-pong (déduplication des buffers transférés dans `encodeInWorker`) — **à préserver tel quel** dans le rework de la Task 7. `preview.ts` a `drawFrame(t, showOverlay?)`. `animPanel.ts` re-rend sur signature de kinds. `buildFilterString` prend `{brightness, contrast, saturation}`.

**Conventions :** commits `feat:`/`fix:`/`test:`/`docs:` terminés par une ligne vide puis `Claude-Session: https://claude.ai/code/session_01LzgHmX6RqUCZ4aE69kKgTv` ; chaque tâche laisse `npm test` et `npm run build` verts ; textes UI en français.

---

### Task 1: Timeline — frameOrder (reverse) et decimate

**Files:**
- Modify: `src/effects/timeline.ts`
- Test: `src/effects/timeline.test.ts`

- [ ] **Step 1: Tests rouges**

Ajouter à la fin de `src/effects/timeline.test.ts` (compléter l'import existant avec `decimate, frameOrder`) :

```ts
describe('frameOrder', () => {
  it('ordre normal', () => {
    expect(frameOrder(4, false, false)).toEqual([0, 1, 2, 3]);
  });
  it('inversé', () => {
    expect(frameOrder(4, true, false)).toEqual([3, 2, 1, 0]);
  });
  it('ping-pong', () => {
    expect(frameOrder(4, false, true)).toEqual([0, 1, 2, 3, 2, 1]);
  });
  it('inversé puis ping-pong (le ping-pong s\'applique à l\'ordre inversé)', () => {
    expect(frameOrder(4, true, true)).toEqual([3, 2, 1, 0, 1, 2]);
  });
});

describe('decimate', () => {
  it('n=1 : identité', () => {
    expect(decimate([0, 1, 2], 80, 1)).toEqual({ order: [0, 1, 2], delayMs: 80 });
  });
  it('n=2 : une frame sur deux, délai doublé (durée totale préservée)', () => {
    expect(decimate([0, 1, 2, 3, 2, 1], 80, 2)).toEqual({ order: [0, 2, 2], delayMs: 160 });
  });
  it('n=3 : une frame sur trois, délai triplé', () => {
    expect(decimate([0, 1, 2, 3, 4, 5], 100, 3)).toEqual({ order: [0, 3], delayMs: 300 });
  });
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `npx vitest run src/effects/timeline.test.ts`
Expected: FAIL (`frameOrder`/`decimate` non exportés).

- [ ] **Step 3: Implémenter**

Dans `src/effects/timeline.ts`, remplacer `pingPongOrder` par la version ci-dessous et ajouter `frameOrder`/`decimate` (garder `sampleTimes` et `delayToFps` inchangés) :

```ts
// Ordre des frames : ordre de base (éventuellement inversé), puis aller-retour
// ping-pong sans dupliquer les extrémités.
export function frameOrder(frameCount: number, reverse: boolean, pingpong: boolean): number[] {
  const base = Array.from({ length: frameCount }, (_, i) => (reverse ? frameCount - 1 - i : i));
  if (!pingpong) return base;
  return [...base, ...base.slice(1, -1).reverse()];
}

// Conservé pour compat (tests v1) : ping-pong sur l'ordre normal.
export const pingPongOrder = (frameCount: number) => frameOrder(frameCount, false, true);

// Réduction de poids : ne garde qu'une frame sur n de l'ordre FINAL et multiplie le
// délai par n — la durée totale de l'animation est préservée.
export function decimate(
  order: number[],
  delayMs: number,
  n: 1 | 2 | 3,
): { order: number[]; delayMs: number } {
  if (n === 1) return { order, delayMs };
  return { order: order.filter((_, i) => i % n === 0), delayMs: delayMs * n };
}
```

- [ ] **Step 4: Vérifier le vert, commit**

Run: `npm test`
Expected: PASS — 38 tests (31 + 7 nouveaux), les tests v1 de `pingPongOrder` restent verts.

```bash
git add src/effects/timeline.ts src/effects/timeline.test.ts
git commit -m "feat: ordre des frames inversable et décimation (moteur pur)"
```

---

### Task 2: Effet flip 3D (`spin3d`)

**Files:**
- Modify: `src/types.ts`, `src/effects/effects.ts`, `src/effects/matrix.ts` (commentaire)
- Test: `src/effects/effects.test.ts`

- [ ] **Step 1: Ajouter le type**

Dans `src/types.ts`, ajouter une variante à `Effect` (après la ligne `bounce`) :

```ts
  | { kind: 'spin3d'; axis: 'x' | 'y'; turns: number; easing: Easing };
```

(La variante `bounce` perd son `;` final au profit de la nouvelle ligne.)

- [ ] **Step 2: Tests rouges**

Ajouter dans `src/effects/effects.test.ts`, à la fin du `describe('effectMatrix', ...)` :

```ts
  it('spin3d axe Y : identité à t=0, effondré au quart de tour, miroir à mi-tour', () => {
    const e: Effect = { kind: 'spin3d', axis: 'y', turns: 1, easing: 'linear' };
    const sq = { imageW: 100, imageH: 100, outW: 100, outH: 100 };
    expect(apply(effectMatrix(e, 0, sq), { x: 10, y: 10 })).toEqual({ x: 10, y: 10 });
    // quart de tour : cos(π/2) ≈ 0 → tout converge vers le centre en x
    expect(apply(effectMatrix(e, 0.25, sq), { x: 10, y: 10 }).x).toBeCloseTo(50);
    // mi-tour : cos(π) = −1 → miroir autour du centre, y intact
    const p = apply(effectMatrix(e, 0.5, sq), { x: 10, y: 10 });
    expect(p.x).toBeCloseTo(90);
    expect(p.y).toBeCloseTo(10);
  });
  it('spin3d axe X écrase la dimension verticale', () => {
    const e: Effect = { kind: 'spin3d', axis: 'x', turns: 1, easing: 'linear' };
    const sq = { imageW: 100, imageH: 100, outW: 100, outH: 100 };
    const p = apply(effectMatrix(e, 0.5, sq), { x: 10, y: 10 });
    expect(p.x).toBeCloseTo(10);
    expect(p.y).toBeCloseTo(90);
  });
```

- [ ] **Step 3: Vérifier l'échec**

Run: `npx vitest run src/effects/effects.test.ts`
Expected: FAIL — TypeScript refuse la variante inconnue, ou le switch de `effectMatrix` ne la gère pas (erreur de compilation attendue : le switch n'est plus exhaustif).

- [ ] **Step 4: Implémenter**

Dans `src/effects/effects.ts` :
1. Étendre l'import : `import { multiply, rotationDeg, scaling, translation, type Mat } from './matrix';`
2. Ajouter un cas au switch de `effectMatrix` (après le cas `bounce`) :

```ts
    case 'spin3d': {
      // Projection parallèle d'une rotation 3D : écrasement en cos de l'axe concerné.
      // Au-delà de 90°, le facteur devient négatif : l'image apparaît en miroir
      // (comportement « carte translucide », assumé par la spec v2).
      const s = Math.cos(2 * Math.PI * effect.turns * applyEasing(effect.easing, t));
      return effect.axis === 'y'
        ? scaling(s, 1, view.outW / 2, view.outH / 2)
        : scaling(1, s, view.outW / 2, view.outH / 2);
    }
```

3. Dans `src/effects/matrix.ts`, mettre à jour le commentaire au-dessus de `identity` (scaling a maintenant un consommateur) :

```ts
// identity complète l'API d'algèbre (utilisé par les tests) ; scaling sert à spin3d.
```

- [ ] **Step 5: Vérifier le vert, commit**

Run: `npm test`
Expected: PASS — 40 tests.

```bash
git add src/types.ts src/effects/effects.ts src/effects/effects.test.ts src/effects/matrix.ts
git commit -m "feat: effet flip 3D (spin3d, projection parallèle en cos)"
```

---

### Task 3: Filtres natifs — types, état initial, buildFilterString

**Files:**
- Modify: `src/types.ts`, `src/state.ts`, `src/adjustments.ts`
- Test: `src/adjustments.test.ts`

- [ ] **Step 1: Étendre Adjustments**

Dans `src/types.ts`, remplacer l'interface `Adjustments` par :

```ts
export interface Adjustments {
  brightness: number;   // 1 = neutre
  contrast: number;     // 1 = neutre
  saturation: number;   // 1 = neutre
  sepia: number;        // 0 = neutre, 0–1
  grayscale: number;    // 0 = neutre, 0–1
  hueRotate: number;    // degrés, 0 = neutre
  blur: number;         // px, 0 = neutre
  invert: boolean;
  flipH: boolean;
  flipV: boolean;
  rotate90: 0 | 1 | 2 | 3; // quarts de tour horaires
  backgroundColor: string; // couleur CSS, ex. '#ffffff'
}
```

Dans `src/state.ts`, compléter `initialState().adjustments` :

```ts
    adjustments: {
      brightness: 1, contrast: 1, saturation: 1,
      sepia: 0, grayscale: 0, hueRotate: 0, blur: 0, invert: false,
      flipH: false, flipV: false, rotate90: 0,
      backgroundColor: '#000000',
    },
```

- [ ] **Step 2: Tests rouges**

Ajouter à la fin du `describe('buildFilterString', ...)` de `src/adjustments.test.ts` :

```ts
  it('nouveaux filtres avec les bonnes unités, dans l\'ordre documenté', () => {
    expect(buildFilterString({
      brightness: 1, contrast: 1, saturation: 1,
      sepia: 0.4, grayscale: 0.3, hueRotate: 90, blur: 2, invert: true,
    })).toBe('sepia(0.4) grayscale(0.3) hue-rotate(90deg) invert(1) blur(2px)');
  });
  it('nouveaux filtres neutres omis', () => {
    expect(buildFilterString({
      brightness: 1, contrast: 1, saturation: 1,
      sepia: 0, grayscale: 0, hueRotate: 0, blur: 0, invert: false,
    })).toBe('none');
  });
```

- [ ] **Step 3: Vérifier l'échec**

Run: `npx vitest run src/adjustments.test.ts`
Expected: FAIL (les nouveaux filtres ne sont pas émis).

- [ ] **Step 4: Implémenter**

Dans `src/adjustments.ts`, remplacer `buildFilterString` par :

```ts
export interface FilterSettings {
  brightness: number;
  contrast: number;
  saturation: number;
  sepia?: number;
  grayscale?: number;
  hueRotate?: number;
  blur?: number;
  invert?: boolean;
}

// L'ordre d'application des filtres CSS est significatif — ordre documenté (spec v2) :
// luminosité, contraste, saturation, sépia, N&B, teinte, inversion, flou.
export function buildFilterString(a: FilterSettings): string {
  const parts: string[] = [];
  if (a.brightness !== 1) parts.push(`brightness(${a.brightness})`);
  if (a.contrast !== 1) parts.push(`contrast(${a.contrast})`);
  if (a.saturation !== 1) parts.push(`saturate(${a.saturation})`);
  if (a.sepia) parts.push(`sepia(${a.sepia})`);
  if (a.grayscale) parts.push(`grayscale(${a.grayscale})`);
  if (a.hueRotate) parts.push(`hue-rotate(${a.hueRotate}deg)`);
  if (a.invert) parts.push('invert(1)');
  if (a.blur) parts.push(`blur(${a.blur}px)`);
  return parts.length ? parts.join(' ') : 'none';
}
```

(`bakeAdjustments` passe déjà l'objet `Adjustments` complet à `buildFilterString` : aucun autre changement.)

- [ ] **Step 5: Vérifier le vert, commit**

Run: `npm test && npm run build`
Expected: PASS — 42 tests ; build propre (l'état initial étendu compile).

```bash
git add src/types.ts src/state.ts src/adjustments.ts src/adjustments.test.ts
git commit -m "feat: cinq filtres natifs (sépia, N&B, teinte, flou, inversion) dans le pipeline de bake"
```

---

### Task 4: UI des filtres

**Files:**
- Modify: `index.html`, `src/ui/sourcePanel.ts`

Pas de test unitaire (DOM) — vérification manuelle en fin de tâche, vérification navigateur complète par le contrôleur après la Task 8.

- [ ] **Step 1: Sliders et case dans index.html**

Dans le bloc `<div id="adjustments" hidden>`, insérer après le label Saturation :

```html
        <label>Sépia <input id="adj-sepia" type="range" min="0" max="1" step="0.05" value="0" /></label>
        <label>Noir &amp; blanc <input id="adj-grayscale" type="range" min="0" max="1" step="0.05" value="0" /></label>
        <label>Teinte <input id="adj-hue" type="range" min="0" max="360" step="5" value="0" /></label>
        <label>Flou <input id="adj-blur" type="range" min="0" max="10" step="0.5" value="0" /></label>
        <label><input id="adj-invert" type="checkbox" /> Inverser les couleurs</label>
```

- [ ] **Step 2: Bindings dans sourcePanel.ts**

Dans `initSourcePanel`, élargir le type de clé du helper `bind` existant et ajouter les bindings après `bind('#adj-saturation', 'saturation');` :

```ts
  const bind = (id: string, key: 'brightness' | 'contrast' | 'saturation' | 'sepia' | 'grayscale' | 'hueRotate' | 'blur') => {
```

```ts
  bind('#adj-sepia', 'sepia');
  bind('#adj-grayscale', 'grayscale');
  bind('#adj-hue', 'hueRotate');
  bind('#adj-blur', 'blur');
  document.querySelector<HTMLInputElement>('#adj-invert')!.addEventListener('change', (e) => {
    store.update({ adjustments: { ...store.get().adjustments, invert: (e.target as HTMLInputElement).checked } });
  });
```

- [ ] **Step 3: Vérification**

Run: `npm test && npm run build`
Expected: verts. Puis `npm run dev`, importer une image : sépia à 1 → teinte brune ; N&B à 1 → gris ; teinte à 180 → couleurs inversées sur la roue ; flou visible ; case inversion → négatif. Arrêter le serveur.

- [ ] **Step 4: Commit**

```bash
git add index.html src/ui/sourcePanel.ts
git commit -m "feat: UI des filtres (sépia, N&B, teinte, flou, inversion)"
```

---

### Task 5: État et UI — spin3d, lecture inversée, décimation

**Files:**
- Modify: `src/state.ts`, `index.html`, `src/ui/animPanel.ts`, `src/export.ts` (listener décimation)

- [ ] **Step 1: Champs d'état**

Dans `src/state.ts`, ajouter à `AppState` (après `loopCount`) :

```ts
  reverse: boolean;   // lecture inversée (preview + export)
  decimation: 1 | 2 | 3; // export : 1 frame sur n, délai × n
```

et à `initialState()` (après `loopCount: 1,`) :

```ts
    reverse: false,
    decimation: 1,
```

- [ ] **Step 2: index.html**

Dans `#effect-buttons`, ajouter :

```html
          <button data-effect="spin3d">+ Flip 3D</button>
```

Après le label Steps :

```html
        <label><input id="anim-reverse" type="checkbox" /> Lecture inversée</label>
```

Dans le bloc export, après le label Qualité :

```html
        <label>Frames exportées
          <select id="out-decimation">
            <option value="1">Toutes</option>
            <option value="2">1 sur 2</option>
            <option value="3">1 sur 3</option>
          </select>
        </label>
```

- [ ] **Step 3: animPanel.ts — carte spin3d et case reverse**

1. Dans `defaultEffect`, ajouter un cas avant `default:` :

```ts
    case 'spin3d':
      return { kind: 'spin3d', axis: 'y', turns: 1, easing: 'linear' };
```

2. Dans `TITLES` : `spin3d: 'Flip 3D',`
3. Dans le switch de `renderEffectCard`, ajouter :

```ts
    case 'spin3d': {
      const axis = document.createElement('label');
      axis.textContent = 'Axe ';
      const select = document.createElement('select');
      for (const [v, txt] of [['y', 'Vertical (Y)'], ['x', 'Horizontal (X)']] as const) {
        const opt = document.createElement('option');
        opt.value = v; opt.textContent = txt; opt.selected = v === effect.axis;
        select.appendChild(opt);
      }
      select.addEventListener('change', () => patch({ axis: select.value as 'x' | 'y' }));
      axis.appendChild(select);
      card.appendChild(axis);
      card.appendChild(numberField('Tours', effect.turns, (turns) => patch({ turns: Math.max(0.25, turns) })));
      card.appendChild(easingField(effect.easing, (easing) => patch({ easing })));
      break;
    }
```

4. Dans `initAnimPanel`, après le listener `#anim-steps` :

```ts
  document.querySelector<HTMLInputElement>('#anim-reverse')!.addEventListener('change', (e) => {
    store.update({ reverse: (e.target as HTMLInputElement).checked });
  });
```

- [ ] **Step 4: export.ts — listener décimation**

Dans `initExport`, après le listener `quality` :

```ts
  document.querySelector<HTMLSelectElement>('#out-decimation')!.addEventListener('change', (e) => {
    store.update({ decimation: Number((e.target as HTMLSelectElement).value) as 1 | 2 | 3 });
  });
```

- [ ] **Step 5: Vérification et commit**

Run: `npm test && npm run build`
Expected: verts. `npm run dev` : le bouton « + Flip 3D » crée une carte (axe, tours, easing) ; la case et le sélecteur existent. Arrêter le serveur.

```bash
git add src/state.ts index.html src/ui/animPanel.ts src/export.ts
git commit -m "feat: état et contrôles UI (flip 3D, lecture inversée, décimation)"
```

---

### Task 6: Preview — reverse et arrêt « n fois »

**Files:**
- Modify: `src/preview.ts`

- [ ] **Step 1: Implémenter**

Dans `src/preview.ts` :
1. Remplacer l'import timeline par : `import { frameOrder, sampleTimes } from './effects/timeline';`
2. Supprimer la fonction `frameSequence()` et remplacer `tick()` par :

```ts
  function tick() {
    const s = store.get();
    const seq = frameOrder(s.steps, s.reverse, s.loopMode === 'pingpong');
    // Mode « n fois » : la lecture s'arrête d'elle-même après loopCount passages complets.
    if (s.loopMode === 'count' && frameIdx >= seq.length * s.loopCount) {
      stop();
      return;
    }
    const times = sampleTimes(s.steps);
    drawFrame(times[seq[frameIdx % seq.length]], false);
    frameIdx++;
    timer = window.setTimeout(tick, s.delayMs);
  }
```

- [ ] **Step 2: Vérification et commit**

Run: `npm test && npm run build` → verts. `npm run dev` : case « Lecture inversée » cochée → l'animation joue à rebours ; mode « n fois » = 2 → la lecture s'arrête seule après 2 passages (le bouton revient à « ▶ Lecture »). Arrêter le serveur.

```bash
git add src/preview.ts
git commit -m "feat: lecture inversée en preview et arrêt effectif du mode n fois"
```

---

### Task 7: Export — rendu par tranches, annulation, reverse + décimation

**Files:**
- Modify: `src/frameGenerator.ts`, `src/export.ts`, `index.html`

**PRÉSERVER** : la déduplication des buffers transférés dans `encodeInWorker` (hotfix cfe3bc0 — le ping-pong duplique des frames, un buffer ne se transfère qu'une fois) et le commentaire/mapping `repeat` (loopCount−1 / −1).

- [ ] **Step 1: Bouton Annuler dans index.html**

Après `<button id="btn-export">Exporter le GIF</button>` :

```html
        <button id="btn-cancel-export" hidden>Annuler</button>
```

- [ ] **Step 2: frameGenerator.ts — rendu par tranches**

Remplacer `renderFrames` par (seul l'export la consommait ; `computeFrameMatrices` inchangé) :

```ts
// Rend les frames par tranches en cédant la main entre chaque tranche : l'UI reste
// réactive, la progression s'affiche, et l'annulation est possible. Retourne null si annulé.
// Navigateur uniquement (non couvert par Vitest) — vérifié lors de l'export.
export async function renderFramesChunked(
  baked: HTMLCanvasElement,
  matrices: Mat[],
  outW: number,
  outH: number,
  backgroundColor: string,
  onProgress: (done: number, total: number) => void,
  isCancelled: () => boolean,
  chunkSize = 4,
): Promise<ImageData[] | null> {
  const canvas = document.createElement('canvas');
  canvas.width = outW;
  canvas.height = outH;
  const ctx = canvas.getContext('2d', { willReadFrequently: true })!;
  const frames: ImageData[] = [];
  for (let i = 0; i < matrices.length; i++) {
    if (isCancelled()) return null;
    const m = matrices[i];
    ctx.setTransform(1, 0, 0, 1, 0, 0);
    ctx.fillStyle = backgroundColor;
    ctx.fillRect(0, 0, outW, outH);
    ctx.setTransform(m.a, m.b, m.c, m.d, m.e, m.f);
    ctx.drawImage(baked, 0, 0);
    frames.push(ctx.getImageData(0, 0, outW, outH));
    if ((i + 1) % chunkSize === 0 || i === matrices.length - 1) {
      onProgress(i + 1, matrices.length);
      await new Promise((r) => setTimeout(r, 0)); // cède la main (repaint + événements)
    }
  }
  return frames;
}
```

- [ ] **Step 3: export.ts — rework du handler**

1. Imports : remplacer `pingPongOrder` par `decimate, frameOrder` ; remplacer `renderFrames` par `renderFramesChunked`.
2. Ajouter en haut du module (après `clampDim`) :

```ts
class ExportCancelled extends Error {}
```

3. Dans `initExport`, ajouter les refs et l'état d'annulation (après la ref `quality`) :

```ts
  const cancelBtn = document.querySelector<HTMLButtonElement>('#btn-cancel-export')!;
  let cancelled = false;
  let cancelEncode: (() => void) | null = null;
  cancelBtn.addEventListener('click', () => {
    cancelled = true;      // interrompt la boucle de rendu
    cancelEncode?.();      // termine le worker si l'encodage a commencé
  });
```

4. Remplacer le corps du listener `btn.addEventListener('click', async () => { ... })` par :

```ts
    const s = store.get();
    if (!s.sourceImage) return;

    const { order, delayMs } = decimate(
      frameOrder(s.steps, s.reverse, s.loopMode === 'pingpong'),
      s.delayMs,
      s.decimation,
    );
    // Pic mémoire réel ≈ 3× les frames brutes : frames + concaténation worker + copie wasm.
    const bytes = order.length * s.outW * s.outH * 4 * 3;
    if (bytes > MAX_BYTES) {
      const go = confirm(
        `Export volumineux : ${order.length} frames en ${s.outW}×${s.outH} (~${Math.round(bytes / 1e6)} Mo en mémoire au pic). Continuer ?`,
      );
      if (!go) return;
    }

    btn.disabled = true;
    cancelBtn.hidden = false;
    cancelled = false;
    progress.hidden = false;
    progress.max = 1;
    progress.value = 0;
    try {
      const baked = await bakeAdjustments(s.sourceImage, s.bgRemovedBlob, s.adjustments);
      const view = { imageW: baked.width, imageH: baked.height, outW: s.outW, outH: s.outH };
      const matrices = computeFrameMatrices(s.effects, s.steps, view);
      const stepFrames = await renderFramesChunked(
        baked, matrices, s.outW, s.outH, s.adjustments.backgroundColor,
        (done, total) => {
          progress.value = done / total;
          status.textContent = `Rendu des frames… (${done}/${total})`;
        },
        () => cancelled,
      );
      if (!stepFrames) {
        status.textContent = 'Export annulé.';
        return;
      }
      const frames = order.map((i) => stepFrames[i]);

      status.textContent = 'Encodage gifski… (peut prendre du temps)';
      progress.removeAttribute('value'); // indéterminée : gifski ne remonte pas de progression
      // repeat : vérifié sur pièce dans le binaire gifski-wasm 2.2.0 installé (rust/src/lib.rs
      // du paquet, et `gif` crate 0.13.1 qu'il utilise en interne) — la doc npm ne précise pas
      // la sémantique. gifski-wasm mappe repeat<0 (ou absent) → boucle infinie ; repeat>=0 →
      // Repeat::Finite(repeat), et Finite(0) fait explicitement sauter l'écriture de
      // l'extension NETSCAPE2.0 (donc AUCUNE boucle, pas une boucle infinie comme le
      // supposait le plan initial). On mappe donc loopCount (nombre total de lectures
      // voulues, "n fois" dans l'UI, toujours ≥ 1) sur loopCount-1 répétitions supplémentaires,
      // et -1 pour les modes infinite/pingpong.
      const repeat = s.loopMode === 'count' ? s.loopCount - 1 : -1;
      const job = encodeInWorker({
        frames, width: s.outW, height: s.outH,
        frameDurations: frames.map(() => delayMs),
        quality: s.quality, repeat,
      });
      cancelEncode = job.cancel;
      const gif = await job.promise;

      const blob = new Blob([gif], { type: 'image/gif' });
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = 'animation.gif';
      a.click();
      URL.revokeObjectURL(a.href);
      status.textContent = `GIF exporté (${(blob.size / 1024).toFixed(0)} Ko).`;
    } catch (err) {
      if (err instanceof ExportCancelled) status.textContent = 'Export annulé.';
      else status.textContent = `Échec de l'export : ${err instanceof Error ? err.message : err}`;
    } finally {
      cancelEncode = null;
      btn.disabled = false;
      cancelBtn.hidden = true;
      progress.hidden = true;
      progress.value = 0;
    }
```

5. Remplacer `encodeInWorker` par une version annulable (la déduplication des buffers est reprise TELLE QUELLE du hotfix) :

```ts
function encodeInWorker(payload: {
  frames: ImageData[]; width: number; height: number;
  frameDurations: number[]; quality: number; repeat: number;
}): { promise: Promise<Uint8Array<ArrayBuffer>>; cancel: () => void } {
  let worker: Worker | null = new Worker(
    new URL('./workers/gifEncoder.worker.ts', import.meta.url),
    { type: 'module' },
  );
  let rejectFn: (e: Error) => void = () => {};
  const promise = new Promise<Uint8Array<ArrayBuffer>>((resolve, reject) => {
    rejectFn = reject;
    worker!.onmessage = (e) => {
      worker?.terminate();
      worker = null;
      if (e.data.ok) resolve(e.data.gif);
      else reject(new Error(e.data.error));
    };
    worker!.onerror = (e) => {
      worker?.terminate();
      worker = null;
      reject(new Error(e.message));
    };
    // Envoie des vues RGBA brutes et TRANSFÈRE leurs buffers : pas de clonage des pixels.
    // Le ping-pong référence deux fois les mêmes ImageData ; un buffer ne pouvant être
    // transféré qu'une seule fois, les occurrences suivantes sont copiées.
    const seen = new Set<ArrayBuffer>();
    const frames = payload.frames.map((f) => {
      const buffer = f.data.buffer as ArrayBuffer;
      if (seen.has(buffer)) return new Uint8Array(f.data); // copie : frame dupliquée
      seen.add(buffer);
      return new Uint8Array(buffer, 0, f.data.byteLength);
    });
    worker!.postMessage({ ...payload, frames }, [...seen]);
  });
  return {
    promise,
    cancel: () => {
      if (worker) {
        worker.terminate();
        worker = null;
        rejectFn(new ExportCancelled('Export annulé'));
      }
    },
  };
}
```

- [ ] **Step 4: Vérification et commit**

Run: `npm test && npm run build`
Expected: verts (42 tests, worker bundlé).
`npm run dev` puis :
1. Export normal → statut « Rendu des frames… (k/n) » avec barre déterminée, puis « Encodage gifski… » indéterminé, puis téléchargement.
2. Export ping-pong + « Lecture inversée » + décimation « 1 sur 2 » → GIF téléchargé (le contrôleur dissèquera le fichier).
3. Cliquer Annuler pendant le rendu (grosse taille, ex. 2048px, 120 steps) → « Export annulé. », pas de téléchargement, bouton réactivé.
Arrêter le serveur.

```bash
git add src/frameGenerator.ts src/export.ts index.html
git commit -m "feat: export annulable avec rendu par tranches, reverse et décimation"
```

---

### Task 8: Documentation

**Files:**
- Modify: `README.md`, `docs/superpowers/smoke-test.md`

- [ ] **Step 1: README**

Dans « Fonctionnalités » : ajouter « flip 3D » à la liste des effets ; ajouter à la liste :

```markdown
- Filtres : sépia, noir & blanc, teinte, flou, inversion
- Lecture inversée ; décimation de frames à l'export (poids réduit, durée préservée)
```

Dans « Limitations connues (v1) » : renommer la section « Limitations connues » et SUPPRIMER les deux puces « rendu synchrone » et « n fois en preview » (corrigées par ce lot). Garder la puce isnet_quint8.

- [ ] **Step 2: Smoke test**

Ajouter à `docs/superpowers/smoke-test.md` :

```markdown
11. Sépia à 1 puis N&B à 1 → aperçu suit ; inversion → négatif.
12. Ajouter Flip 3D (axe Y, 1 tour) → ▶ : l'image « tourne » (écrasement/miroir).
13. Cocher « Lecture inversée » → l'animation joue à rebours (preview et GIF exporté).
14. Décimation « 1 sur 2 » → GIF exporté 2× moins de frames, même durée totale.
15. Lancer un gros export (2048 px, 120 steps) puis Annuler pendant le rendu →
    « Export annulé. », pas de téléchargement, bouton réactivé.
```

- [ ] **Step 3: Commit et push**

```bash
git add README.md docs/superpowers/smoke-test.md
git commit -m "docs: fonctionnalités v2 lot 1 et smoke test étendu"
git push
```

---

## Couverture spec v2 → tâches

| Exigence spec v2 | Tâche |
|---|---|
| Reverse (moteur, preview, export, case UI) | 1, 5, 6, 7 |
| Flip 3D spin3d (type, matrice, tests, carte UI, bouton) | 2, 5 |
| Décimation (moteur testé, sélecteur, export, garde-fou décimé) | 1, 5, 7 |
| 5 filtres natifs (types, état, buildFilterString testé, sliders) | 3, 4 |
| « n fois » s'arrête en preview | 6 |
| Rendu chunké + progression déterminée + Annuler (rendu ET encodage) | 7 |
| README (retrait des 2 limitations) + smoke test | 8 |
| Préservation hotfix ping-pong et mapping repeat | 7 (explicite) |
